#include <stdio.h>

int main() {
    int dividend = 10;
    int divisor = 2;
    int quotient;
    quotient = dividend / divisor;
    printf("The quotient of %d divided by %d is %d\n", dividend, divisor, quotient);

    return 0;
}


